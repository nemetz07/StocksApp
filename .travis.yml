language: android
dist: trusty

before_install:
  - yes | sdkmanager "platforms;android-30"
  - yes | sdkmanager "build-tools;30.0.3"

android:
  components:
    - tools
    - platform-tools
    - build-tools-30.0.3
    - android-30
  licenses:
    - '.+'